# build output
/bin/
/dist/
/cassette
/cassette-*

# local state (cassettes themselves ARE committed, see CASSETTE_PLAN.md §3)
.cassette/analytics/
.cassette/tmp/
*.cas.tmp

# go
*.test
*.out
coverage.*

# node (web UI, phase 7)
node_modules/
# The production bundle is checked in because go:embed needs it in a clean
# Go-only checkout. npm still owns and regenerates these files.

# os
.DS_Store

/web/site-dist/
